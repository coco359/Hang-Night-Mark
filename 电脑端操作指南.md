# 电脑端操作指南

## 一、如何写笔记

### 方法1：在GitHub网页端直接编写

1. 登录GitHub账号
2. 进入你的 `用户名.github.io` 仓库
3. 点击对应的文件夹（如 `01_自我焦虑与和解`）
4. 点击 `Add file` → `Create new file`
5. 文件名以 `.md` 结尾（如 `2024-01-01-内卷焦虑.md`）
6. 复制对应文件夹中的 `模板.md` 内容
7. 填写笔记内容
8. 拉到页面底部，在 `Commit new file` 区域填写提交信息（如 "添加新笔记"）
9. 点击 `Commit new file` 按钮

### 方法2：在本地编写后上传

1. 在本地电脑上创建一个Markdown文件（.md后缀）
2. 使用Markdown编辑器（如 Typora、VS Code、Notepad++等）编写内容
3. 登录GitHub账号
4. 进入你的 `用户名.github.io` 仓库
5. 点击对应的文件夹
6. 点击 `Add file` → `Upload files`
7. 拖拽或选择本地的Markdown文件
8. 拉到页面底部，填写提交信息
9. 点击 `Commit changes` 按钮

## 二、如何修改旧内容

1. 登录GitHub账号
2. 进入你的 `用户名.github.io` 仓库
3. 找到需要修改的Markdown文件
4. 点击文件，然后点击右上角的铅笔图标（Edit）
5. 修改内容
6. 拉到页面底部，填写提交信息（如 "修改笔记内容"）
7. 点击 `Commit changes` 按钮

## 三、如何同步更新网站

GitHub Pages会自动同步你的仓库内容，通常在你提交后几分钟内完成更新。无需手动操作。

## 四、如何查看更新后的网站

在浏览器中访问 `https://用户名.github.io` 即可查看更新后的网站。

## 五、推荐的Markdown编辑器

- **Typora**：所见即所得，操作简单
- **VS Code**：功能强大，支持插件
- **Notepad++**：轻量级，适合简单编辑
- **在线编辑器**：如 StackEdit、Dillinger 等